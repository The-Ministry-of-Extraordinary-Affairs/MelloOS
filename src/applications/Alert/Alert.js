import Window from '../../components/Window/Window';
import { Component } from 'preact';

class Alert extends Component {
    nope = () => { return}

    render(props) {
        return ( <Window> ALERT </Window> )
    }
}

export default Alert;